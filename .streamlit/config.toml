[theme]
primaryColor              = "#C4813A"   # amber — buttons, links, accents
backgroundColor           = "#FFFFFF"   # white — main area
secondaryBackgroundColor  = "#F7F7F7"   # light grey — sidebar
textColor                 = "#1C1C1E"   # near-black — body text
font                      = "sans serif"
